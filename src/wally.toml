[package]
name = "solarscuffle-bot/priority-queue"
version = "1.0.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

description = "A procedural min-sorted priority-queue using an indirect binary heap."

[dependencies]